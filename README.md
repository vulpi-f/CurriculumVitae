# Curriculum Vitae

LaTeX source for Fabio Vulpi's one-page CV.

## Build

```sh
latexmk cv.tex
```

The generated file is `cv.pdf`.

## Structure

- `cv.tex`: main LaTeX source.
- `FIGURES/CV_picture.JPG`: profile photo used in the header.
